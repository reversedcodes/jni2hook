/* What the running VM actually has, standing in for an obfuscated class. Its
   shape matches RemapApi; only the names differ. */
public class ra {

    public String b(rv value) {
        return "api:" + value.d();
    }
}
