/* Readable stand-in for the obfuscated rv. Appears in a method descriptor and
   in a method reference, so remapping has to reach both. */
public class RemapValue {

    public String text() {
        return "compile-time";
    }
}
